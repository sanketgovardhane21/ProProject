{
  "rewrites": [
    {
      "source": "/api/(.*)",
      "destination": "https://your-backend.vercel.app/api/$1"
    }
  ],
  "buildCommand": "npm run build",
  "outputDirectory": "dist", // or "build" depending on your frontend
  "framework": "vite" // or "create-react-app" or "nextjs", etc.
}
